name: CI

on:
  push:
    branches: [main, Latest, master]
  pull_request:
    branches: [main, Latest, master]

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew clean build --no-daemon --stacktrace 2>&1 | tee build.log

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log
          retention-days: 7

      - name: Run unit tests
        run: ./gradlew test --no-daemon --stacktrace 2>&1 | tee test.log

      - name: Upload test log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-log
          path: test.log
          retention-days: 7

      - name: Validate resources
        run: ./gradlew validateResources --no-daemon --stacktrace

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: uncannyvoxel-build
          path: build/libs/*.jar
          retention-days: 7

  ci-verification-gate:
    name: CI Verification Gate
    runs-on: ubuntu-latest
    needs: build-and-test
    if: always()
    timeout-minutes: 10

    steps:
      - name: Check build-and-test job status
        run: |
          if [ "${{ needs.build-and-test.result }}" != "success" ]; then
            echo "CI verification gate FAILED: build-and-test job did not succeed"
            exit 1
          fi
          echo "CI verification gate PASSED: all tests and build succeeded"

  lint-and-validate:
    name: Lint and Validate
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check for common issues
        run: |
          if grep -r "TODO\|FIXME" src/main --include="*.java" | grep -v test; then
            echo "WARNING: TODO/FIXME comments found in production code"
          fi