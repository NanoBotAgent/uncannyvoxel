#!/bin/bash
# Gradle wrapper script - run 'gradle wrapper' to generate this
# For now, use: ./gradlew or gradle wrapper
echo "Run 'gradle wrapper' to generate gradlew script"
echo "Or use system gradle: gradle build"